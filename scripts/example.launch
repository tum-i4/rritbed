<?xml version="1.0"?>
<!-- Exemplary launch file with different node types and how to use them -->
<launch>
  <group ns="turtle">
  	<node name="py_turtlesim" pkg="py_turtlesim" type="py_turtlesim.py" />
    <!-- A random mover with seed pi1000 (31415...) -->
	  <node name="mover" pkg="turtlesim_expl" type="random_mover.py" 
      args="-pi1000" />

    <!-- Manual turtle control -->
    <!-- node pkg="turtlesim" type="turtle_teleop_key" name="teleop" output="screen"/ -->
  </group>

  <!-- Data generation with gaussian distribution and default arguments -->
  <node name="gauss" pkg="turtlesim_expl" type="distribution_publisher.py"
    args="gaussian" />

  <!-- Logging -->
  <node name="logger" pkg="turtlesim_expl" type="logger.py" />

  <!-- Rosbag recording with prefix as given -->
  <node pkg="rosbag" type="record" name="rosbag_recorder"
    args="-a -o /tmp/ros-bag-recording-all"/>
</launch>
